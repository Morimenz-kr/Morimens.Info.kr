rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 게시 사이트는 Firestore를 직접 읽지 않는다. 모든 운영 읽기와 쓰기는
    // Firebase Admin SDK를 사용하는 검증된 배포 작업에서만 수행한다.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
